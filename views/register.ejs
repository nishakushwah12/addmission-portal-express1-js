<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <title>Document</title>
    <style>
        body {
            background-color: #12587a;
        }

        .logo {
            width: 120px;
            height: 120px;
            margin: auto;
            vertical-align: middle;
        }

        .regs_heading {
            letter-spacing: 1px;
            font-weight: 600;
            color: #12587a;
            font-size: 30px;
        }
    </style>
    <div class="container py-5 h-100">
        <div class="row d-flex justify-content-center align-items-center h-100">
            <div class="col col-xl-10">
                <div class="card" style="border-radius:1rem;">
                    <div class="row g-0">
                        <div class="col-md-6 col-lg-5 d-none d-md-block">
                            <img src="https://mdbcdn.b-cdn.net/img/Photos/new-templates/bootstrap-login-form/img1.webp"
                                class="img-fluid" style="border-radius: 1rem 0 0 1rem;">
                        </div>
                        <div class="col-md-6 col-lg-7 d-flex align-items-center">
                            <div class="card-body p-4 text-black">
                                <div class="d-flex align-items-center pb-1">
                                    <img src="images/logo.png" class="logo" />
                                </div>
                                <h5 class="text-center mb-3 pb-3 regs_heading">Register here</h5>
                                
                                <% if(message.length> 0){ %>
                                    <div class="alert alert-danger">
                                        <%= message %>
                                    </div>


                                    <% } %>
                                        <form action="/insertreg" method="post" enctype="multipart/form-data">
                                            <div class="form-group">
                                                <input type="text" placeholder="Name" name="n" class="form-control mt-3"
                                                    required="" fdprocessedid="vlh7crd">
                                            </div>
                                            <div class="form-group">
                                                <input type="email" placeholder="Email" name="e"
                                                    class="form-control mt-3" required="" fdprocessedid="0sbku8">
                                            </div>

                                            <div class="form-group">
                                                <input type="password" placeholder="Password" id="myInput1" name="p"
                                                    class="form-control mt-3" required="" fdprocessedid="gqb82s"
                                                    aria-autocomplete="list">
                                            </div>
                                            <div class="form-group">
                                                <input type="password" placeholder=" confirm Password" id="myInput1"
                                                    name="cp" class="form-control mt-3" required=""
                                                    fdprocessedid="gqb82s" aria-autocomplete="list">
                                            </div>
                                            <input type="checkbox" onclick="myFunction()">
                                            <span>&nbsp;Show Password</span>
                                            <div class="form-group my-2">
                    <label class="form-label">Profile Picture</label>
                    <input type="file" name="image" class="form-control" required="">
                </div>
                                            <div class="form-group text-center">
                                                <button type="submit" class="btn btn-info mt-3 form-control mb-3"
                                                    style="background: #12587a; color: #ffff" fdprocessedid="6rrnvb">
                                                    Register</button>
                                                <span class=" text-center pt-3">HAVE AN ACCOUNT?
                                                    <a style="color: #393f81;" href="/">LOGIN NOW</a>
                                                </span>
                                            </div>
                                        </form>
                            </div>
                        </div>
                    </div>
                </div>

            </div>
        </div>
    </div>
    <!-- Code injected by live-server -->
    <script>
        // <![CDATA[  <-- For SVG support
        if ('WebSocket' in window) {
            (function () {
                function refreshCSS() {
                    var sheets = [].slice.call(document.getElementsByTagName("link"));
                    var head = document.getElementsByTagName("head")[0];
                    for (var i = 0; i < sheets.length; ++i) {
                        var elem = sheets[i];
                        var parent = elem.parentElement || head;
                        parent.removeChild(elem);
                        var rel = elem.rel;
                        if (elem.href && typeof rel != "string" || rel.length == 0 || rel.toLowerCase() == "stylesheet") {
                            var url = elem.href.replace(/(&|\?)_cacheOverride=\d+/, '');
                            elem.href = url + (url.indexOf('?') >= 0 ? '&' : '?') + '_cacheOverride=' + (new Date().valueOf());
                        }
                        parent.appendChild(elem);
                    }
                }
                var protocol = window.location.protocol === 'http:' ? 'ws://' : 'wss://';
                var address = protocol + window.location.host + window.location.pathname + '/ws';
                var socket = new WebSocket(address);
                socket.onmessage = function (msg) {
                    if (msg.data == 'reload') window.location.reload();
                    else if (msg.data == 'refreshcss') refreshCSS();
                };
                if (sessionStorage && !sessionStorage.getItem('IsThisFirstTime_Log_From_LiveServer')) {
                    console.log('Live reload enabled.');
                    sessionStorage.setItem('IsThisFirstTime_Log_From_LiveServer', true);
                }
            })();
        }
        else {
            console.error('Upgrade your browser. This Browser is NOT supported WebSocket for Live-Reloading.');
        }
        // ]]>
    </script>
    </body>

</html>